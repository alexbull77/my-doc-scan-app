import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6750a4",
      contrastText: "#fff",
    },
    secondary: {
      main: "#eaddff",
      contrastText: "#000",
    },
  },
});

export default theme;
